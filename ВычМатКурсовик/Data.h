#include "stdafx.h"
#include <vector>
#include <fstream>
#include <string>
#include <iostream>
using namespace std;

vector<double> GetDoubleElement(string str);
vector<vector<double>> ReadFromFile(char *filename);